import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TuitionTrust - Empowering Education Through Transparent Donations",
  description: "TuitionTrust connects donors with underprivileged schools through transparent, blockchain-backed donations on the XRP Ledger.",
  keywords: ["education", "donation", "blockchain", "XRPL", "schools", "transparency", "XRP", "RLUSD", "decentralized identifiers", "DID"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
